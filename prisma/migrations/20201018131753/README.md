# Migration `20201018131753`

This migration has been generated by Alexander Swed at 10/18/2020, 3:17:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Workspace" ADD COLUMN "github_org_id" text   

CREATE TABLE "public"."Project" (
"id" SERIAL,
"name" text   ,
"workspaceId" integer   ,
"projectSetupId" integer   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."ProjectSetup" (
"id" SERIAL,
"project_id" integer   NOT NULL ,
"shared_secret" text   NOT NULL ,
"active" boolean   NOT NULL DEFAULT true,
"created_at" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Workspace.github_org_id_unique" ON "public"."Workspace"("github_org_id")

ALTER TABLE "public"."Project" ADD FOREIGN KEY ("projectSetupId")REFERENCES "public"."ProjectSetup"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "public"."Project" ADD FOREIGN KEY ("workspaceId")REFERENCES "public"."Workspace"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201017191957..20201018131753
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -64,12 +64,28 @@
 model Workspace {
   id            Int       @id @default(autoincrement())
   name          String?
   users         WorkspaceUser[]
+  projects      Project[]
+  githubOrgId   String?   @unique @map(name: "github_org_id")
   createdAt     DateTime  @default(now()) @map(name: "created_at")
   updatedAt     DateTime  @default(now()) @map(name: "updated_at")
 }
+model Project {
+  id            Int       @id @default(autoincrement())
+  name          String?
+  setup         ProjectSetup?
+}
+
+model ProjectSetup {
+  id            Int       @id @default(autoincrement())
+  projectId     Int       @map(name: "project_id")
+  sharedSecret  String    @default(uuid()) @map(name: "shared_secret")
+  active        Boolean   @default(true)
+  createdAt     DateTime  @default(now()) @map(name: "created_at")
+}
+
 enum Role {
   USER
   ADMIN
 }
```


